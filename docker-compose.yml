services:
  mongodb:
    image: mongo:7
    container_name: vigilant-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: vigilant
      MONGO_INITDB_ROOT_PASSWORD: vigilant
      MONGO_INITDB_DATABASE: vigilant
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
